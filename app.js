const Server = require("./server/config.server");//se importa el server

const server = new Server();//se crea instancia

server.listen();//con esto se inicializa la aplicacion, es decri que no basta con solo ionstanciarlo



